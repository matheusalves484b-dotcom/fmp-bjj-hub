import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, ChevronRight, Edit3, Loader2, Plus, Search, UserRound, X } from "lucide-react";
import { AuthGuard } from "@/components/auth-guard";
import { supabase } from "@/lib/supabase";

type Status="ACTIVE"|"INACTIVE"|"SUSPENDED";
type Student={user_id:string;full_name:string;phone:string|null;birth_date:string|null;cpf:string|null;emergency_contact_name:string|null;emergency_contact_phone:string|null;status:Status;enrollment_date:string;notes:string|null;belt:string;plan:string;payment:string};
const statusLabels:Record<Status,string>={ACTIVE:"Ativo",INACTIVE:"Inativo",SUSPENDED:"Suspenso"};
const emptyStudent=():Student=>({user_id:"",full_name:"",phone:null,birth_date:null,cpf:null,emergency_contact_name:null,emergency_contact_phone:null,status:"ACTIVE",enrollment_date:new Date().toISOString().slice(0,10),notes:null,belt:"Sem graduação",plan:"Sem plano",payment:"Em dia"});
export const Route=createFileRoute("/alunos")({component:StudentsRoute});
function StudentsRoute(){return <AuthGuard allowedRoles={["OWNER","ADMIN","TEACHER"]}><Students/></AuthGuard>}
function Students(){
 const[students,setStudents]=useState<Student[]>([]),[loading,setLoading]=useState(true),[query,setQuery]=useState(""),[status,setStatus]=useState<"ALL"|Status>("ALL"),[selected,setSelected]=useState<Student|null>(null),[editing,setEditing]=useState(false),[error,setError]=useState("");
 const loadStudents=async()=>{setLoading(true);setError("");const{data,error}=await supabase.from("student_profiles").select("user_id,birth_date,cpf,emergency_contact_name,emergency_contact_phone,status,enrollment_date,notes,profiles!student_profiles_user_id_fkey(full_name,phone),student_graduations(belts(name)),enrollments(active,plans(name)),invoices(status)").order("enrollment_date",{ascending:false});if(error){setError(error.message);setLoading(false);return}setStudents((data??[]).map((row:any)=>{const graduation=[...(row.student_graduations??[])].sort((a:any,b:any)=>String(b.graduation_date??"").localeCompare(String(a.graduation_date??"")))[0];const enrollment=(row.enrollments??[]).find((e:any)=>e.active)??row.enrollments?.[0];const overdue=(row.invoices??[]).some((i:any)=>i.status==="OVERDUE");return{...row,full_name:row.profiles?.full_name??"Sem nome",phone:row.profiles?.phone??null,belt:graduation?.belts?.name??"Sem graduação",plan:enrollment?.plans?.name??"Sem plano",payment:overdue?"Em atraso":"Em dia"} as Student}));setLoading(false)};
 useEffect(()=>{void loadStudents()},[]);
 const filtered=useMemo(()=>students.filter(s=>(status==="ALL"||s.status===status)&&`${s.full_name} ${s.cpf??""} ${s.phone??""}`.toLowerCase().includes(query.toLowerCase())),[students,query,status]);
 return <div className="min-h-screen bg-ink text-foreground dashboard-atmosphere"><header className="sticky top-0 z-20 flex h-16 items-center border-b border-hairline bg-obsidian/90 px-4 backdrop-blur sm:px-6"><div><h1 className="text-[15px] font-semibold">Alunos</h1><p className="mt-1 font-mono text-[10px] uppercase tracking-[.15em] text-faint">Gestão de alunos</p></div><button onClick={()=>{setSelected(emptyStudent());setEditing(true)}} className="ml-auto flex h-10 items-center gap-2 rounded-xl bg-primary px-3.5 text-xs font-semibold text-primary-foreground"><Plus className="size-4"/><span className="hidden sm:inline">Novo aluno</span></button></header><main className="mx-auto max-w-[1280px] px-4 py-5 sm:px-6 sm:py-7"><div className="mb-5 grid gap-3 sm:grid-cols-[1fr_auto]"><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-faint"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nome, CPF ou telefone..." className="h-11 w-full rounded-xl border border-hairline bg-panel pl-10 pr-3 text-sm outline-none focus:border-primary"/></div><select value={status} onChange={e=>setStatus(e.target.value as "ALL"|Status)} className="h-11 rounded-xl border border-hairline bg-panel px-3 text-sm"><option value="ALL">Todos os status</option><option value="ACTIVE">Ativos</option><option value="INACTIVE">Inativos</option><option value="SUSPENDED">Suspensos</option></select></div>{error&&<div className="mb-4 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-xs text-red-300">{error}</div>}{loading?<div className="flex min-h-48 items-center justify-center"><Loader2 className="size-6 animate-spin text-primary"/></div>:filtered.length===0?<div className="rounded-2xl border border-dashed border-hairline bg-panel/40 px-6 py-16 text-center"><UserRound className="mx-auto size-8 text-faint"/><p className="mt-3 font-medium">Nenhum aluno encontrado</p><p className="mt-1 text-sm text-faint">Ajuste a busca ou cadastre um novo aluno.</p></div>:<div className="overflow-hidden rounded-2xl border border-hairline bg-panel/50"><div className="hidden grid-cols-[1.6fr_.8fr_.9fr_.8fr_32px] gap-4 border-b border-hairline px-5 py-3 font-mono text-[9px] uppercase tracking-[.16em] text-faint md:grid"><span>Aluno</span><span>Graduação</span><span>Plano</span><span>Pagamento</span><span/></div>{filtered.map(s=><button key={s.user_id} onClick={()=>{setSelected(s);setEditing(false)}} className="grid w-full grid-cols-[1fr_28px] items-center gap-3 border-b border-hairline px-4 py-4 text-left last:border-0 hover:bg-white/[.025] md:grid-cols-[1.6fr_.8fr_.9fr_.8fr_32px] md:px-5"><div className="min-w-0"><p className="truncate text-sm font-medium">{s.full_name}</p><p className="mt-1 truncate text-[11px] text-faint">{s.phone||"Sem telefone"} · <span className={s.status==="ACTIVE"?"text-accent-soft":"text-mist"}>{statusLabels[s.status]}</span></p></div><span className="hidden text-xs text-mist md:block">{s.belt}</span><span className="hidden text-xs text-mist md:block">{s.plan}</span><span className={`hidden text-xs md:block ${s.payment==="Em dia"?"text-accent-soft":"text-red-300"}`}>{s.payment}</span><ChevronRight className="size-4 text-faint"/></button>)}</div>}{selected&&!editing&&<StudentDetail student={selected} onClose={()=>setSelected(null)} onEdit={()=>setEditing(true)}/>} {selected&&editing&&<StudentForm student={selected} onClose={()=>{setEditing(false);if(!selected.user_id)setSelected(null)}} onSaved={()=>{setEditing(false);setSelected(null);void loadStudents()}}/>}</main></div>
}
function StudentDetail({student,onClose,onEdit}:{student:Student;onClose:()=>void;onEdit:()=>void}){const[tab,setTab]=useState("personal");return <div className="fixed inset-0 z-40 bg-black/60 backdrop-blur-sm"><aside className="absolute right-0 top-0 flex h-full w-full max-w-xl flex-col border-l border-hairline bg-obsidian shadow-2xl"><div className="flex h-16 items-center gap-3 border-b border-hairline px-5"><button onClick={onClose} className="rounded-lg p-2 text-faint hover:bg-panel"><ArrowLeft className="size-4"/></button><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{student.full_name}</p><p className="font-mono text-[9px] uppercase tracking-[.16em] text-faint">Perfil do aluno</p></div><button onClick={onEdit} className="flex items-center gap-2 rounded-lg border border-hairline bg-panel px-3 py-2 text-xs font-medium"><Edit3 className="size-3.5"/>Editar</button></div><div className="border-b border-hairline px-5"><div className="flex gap-5 overflow-x-auto">{[["personal","Pessoal"],["graduations","Graduações"],["finance","Financeiro"],["attendance","Frequência"]].map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={tab===id?"border-b-2 border-primary py-3 text-xs font-semibold":"border-b-2 border-transparent py-3 text-xs text-faint"}>{label}</button>)}</div></div><div className="flex-1 overflow-y-auto p-5">{tab==="personal"&&<InfoGrid items={[["Status",statusLabels[student.status]],["Telefone",student.phone||"Não informado"],["CPF",student.cpf||"Não informado"],["Nascimento",student.birth_date||"Não informado"],["Matrícula",student.enrollment_date],["Contato de emergência",student.emergency_contact_name||"Não informado"],["Telefone emergência",student.emergency_contact_phone||"Não informado"]]}/>} {tab==="graduations"&&<InfoGrid items={[["Graduação atual",student.belt],["Próximo passo","Registrar na área Graduações"]]}/>} {tab==="finance"&&<InfoGrid items={[["Plano atual",student.plan],["Situação",student.payment],["Status",student.status==="ACTIVE"?"Matrícula ativa":statusLabels[student.status]]]}/>} {tab==="attendance"&&<InfoGrid items={[["Frequência","Consulte a área Frequência para os check-ins da turma."],["Aluno",student.full_name]]}/>}</div></aside></div>}
function InfoGrid({items}:{items:string[][]}){return <div className="grid gap-3 sm:grid-cols-2">{items.map(([label,value])=><div key={label} className="rounded-xl border border-hairline bg-panel/70 p-4"><p className="font-mono text-[9px] uppercase tracking-[.15em] text-faint">{label}</p><p className="mt-2 text-sm text-mist">{value}</p></div>)}</div>}
function StudentForm({student,onClose,onSaved}:{student:Student;onClose:()=>void;onSaved:()=>void}){const isNew=!student.user_id;const[name,setName]=useState(student.full_name),[phone,setPhone]=useState(student.phone??""),[birth,setBirth]=useState(student.birth_date??""),[cpf,setCpf]=useState(student.cpf??""),[emergencyName,setEmergencyName]=useState(student.emergency_contact_name??""),[emergencyPhone,setEmergencyPhone]=useState(student.emergency_contact_phone??""),[studentStatus,setStudentStatus]=useState<Status>(student.status),[notes,setNotes]=useState(student.notes??""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[saving,setSaving]=useState(false),[error,setError]=useState("");
 const save=async()=>{setSaving(true);setError("");try{if(!name.trim())throw new Error("Informe o nome completo.");if(isNew){if(!email||password.length<6)throw new Error("Informe um e-mail e uma senha com pelo menos 6 caracteres.");const{data,error:e}=await supabase.functions.invoke("admin-create-student",{body:{email,password,full_name:name.trim(),phone,birth_date:birth||null,cpf:cpf||null,emergency_contact_name:emergencyName||null,emergency_contact_phone:emergencyPhone||null,status:studentStatus,enrollment_date:student.enrollment_date,notes:notes||null}});if(e)throw new Error(e.message);if(data?.error)throw new Error(data.error)}else{const{error:e1}=await supabase.from("profiles").update({full_name:name.trim(),phone:phone||null}).eq("id",student.user_id);if(e1)throw e1;const{error:e2}=await supabase.from("student_profiles").update({birth_date:birth||null,cpf:cpf||null,emergency_contact_name:emergencyName||null,emergency_contact_phone:emergencyPhone||null,status:studentStatus,notes:notes||null}).eq("user_id",student.user_id);if(e2)throw e2}onSaved()}catch(e){setError(e instanceof Error?e.message:"Não foi possível salvar o aluno.")}finally{setSaving(false)}};
 return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4 backdrop-blur-sm"><section className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-hairline bg-obsidian shadow-2xl"><div className="flex items-center gap-3 border-b border-hairline px-5 py-4"><div className="grid size-9 place-items-center rounded-xl bg-primary/15 text-accent-soft"><UserRound className="size-4"/></div><div className="flex-1"><h2 className="text-sm font-semibold">{isNew?"Cadastrar aluno":"Editar aluno"}</h2><p className="mt-0.5 text-[11px] text-faint">Dados pessoais e acesso</p></div><button onClick={onClose}><X className="size-4"/></button></div><div className="grid gap-4 p-5 sm:grid-cols-2"><Field label="Nome completo" value={name} onChange={setName} required/><Field label="Telefone" value={phone} onChange={setPhone}/>{isNew&&<><Field label="E-mail de acesso" value={email} onChange={setEmail} type="email" required/><Field label="Senha inicial" value={password} onChange={setPassword} type="password" required/></>}<Field label="Data de nascimento" value={birth} onChange={setBirth} type="date"/><Field label="CPF" value={cpf} onChange={setCpf}/><Field label="Contato de emergência" value={emergencyName} onChange={setEmergencyName}/><Field label="Telefone de emergência" value={emergencyPhone} onChange={setEmergencyPhone}/><label><span className="mb-2 block text-[11px] font-medium text-mist">Status</span><select value={studentStatus} onChange={e=>setStudentStatus(e.target.value as Status)} className="h-11 w-full rounded-xl border border-hairline bg-panel px-3 text-sm"><option value="ACTIVE">Ativo</option><option value="INACTIVE">Inativo</option><option value="SUSPENDED">Suspenso</option></select></label><label className="sm:col-span-2"><span className="mb-2 block text-[11px] font-medium text-mist">Observações</span><textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={3} className="w-full rounded-xl border border-hairline bg-panel px-3 py-2 text-sm outline-none focus:border-primary"/></label></div>{error&&<p className="mx-5 mb-4 rounded-xl border border-red-500/30 bg-red-500/10 px-3 py-2 text-xs text-red-300">{error}</p>}<div className="flex justify-end gap-2 border-t border-hairline p-5"><button onClick={onClose} className="rounded-xl border border-hairline px-4 py-2.5 text-xs font-medium">Cancelar</button><button disabled={saving} onClick={()=>void save()} className="flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-xs font-semibold text-primary-foreground disabled:opacity-60">{saving&&<Loader2 className="size-3.5 animate-spin"/>}Salvar aluno</button></div></section></div>}
function Field({label,value,onChange,type="text",required=false}:{label:string;value:string;onChange:(v:string)=>void;type?:string;required?:boolean}){return <label><span className="mb-2 block text-[11px] font-medium text-mist">{label}</span><input required={required} type={type} value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-xl border border-hairline bg-panel px-3 text-sm outline-none focus:border-primary"/></label>}
