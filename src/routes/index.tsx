import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { CalendarPlus, Check, ChevronDown, CircleDot, LogOut, Plus, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/lib/auth";
import { supabase } from "@/lib/supabase";
import instructorRafael from "@/assets/instructor-rafael.jpg";
import studentThiago from "@/assets/student-thiago.jpg";
import studentCamila from "@/assets/student-camila.jpg";
import studentBruno from "@/assets/student-bruno.jpg";
import studentHelena from "@/assets/student-helena.jpg";

export const Route = createFileRoute("/")({ head: () => ({ meta: [{ title: "Painel — Gestão FMP BJJ" }] }), component: Index });

function Index() {
  const { profile } = useAuth(); const [notice, setNotice] = useState(""); const isStudent = profile?.role === "STUDENT";
  const roleLabel = { OWNER: "Proprietário", ADMIN: "Administrador", TEACHER: "Professor", STUDENT: "Aluno" }[profile?.role ?? "STUDENT"];
  const classes = [["18:30", "Jiu-Jitsu · Fundamentos", "Prof. Rafael · Tatame A", "18/24", true], ["19:30", "Jiu-Jitsu · Sparring", "Prof. Rafael · Tatame A", "9/12", false], ["20:30", "Jiu-Jitsu · Competição", "Prof. Marina · Tatame B", "14/20", false], ["21:30", "Jiu-Jitsu · Avançado", "Prof. Rafael · Tatame A", "11/16", false]] as const;
  const attendees = [[studentThiago, "Thiago Prado", "Jiu-Jitsu · Faixa azul", "07:04", true], [studentCamila, "Camila Rocha", "Jiu-Jitsu · Faixa branca", "06:58", false], [studentBruno, "Bruno Alves", "Jiu-Jitsu · Faixa marrom", "06:41", false], [studentHelena, "Helena Martins", "Jiu-Jitsu · Faixa roxa", "06:30", false]] as const;
  const logout = async () => { await supabase.auth.signOut(); };
  const staffNav = [["Alunos", "/alunos"], ["Turmas & Aulas", "#"], ["Financeiro", "#"], ["Graduações", "#"], ["Configurações", "#"]];
  const studentNav = [["Minhas Aulas", "#"], ["Meus Pagamentos", "#"], ["Minha Graduação", "#"]];
  const navItems = isStudent ? studentNav : staffNav;

  return <div className="min-h-screen bg-ink text-foreground dashboard-atmosphere"><div className="flex min-h-screen"><aside className="hidden w-[232px] shrink-0 border-r border-hairline bg-obsidian/80 md:flex md:flex-col"><div className="flex h-16 items-center gap-2.5 px-5"><div className="grid size-8 place-items-center rounded-[9px] bg-primary font-mono text-sm font-semibold text-primary-foreground">F</div><div className="leading-none"><p className="text-[13px] font-semibold">FMP BJJ</p><p className="mt-0.5 font-mono text-[9px] uppercase tracking-[0.2em] text-faint">Tatame OS</p></div></div><nav className="px-3 text-[13px]"><p className="px-2 pb-2 pt-2 font-mono text-[9px] uppercase tracking-[0.2em] text-faint">{isStudent ? "Área do aluno" : "Operação"}</p><a className="flex items-center gap-2.5 rounded-[10px] bg-primary/15 px-3 py-2 font-medium ring-1 ring-inset ring-primary/25"><CircleDot className="size-3 text-accent-soft" />{isStudent ? "Resumo" : "Visão geral"}</a>{navItems.map(([item, href]) => href === "/alunos" ? <Link key={item} to="/alunos" className="mt-0.5 flex items-center gap-2.5 rounded-[10px] px-3 py-2 text-mist hover:bg-panel hover:text-foreground"><span className="size-1.5 rounded-full bg-hairline" />{item}</Link> : <a key={item} href={href} className="mt-0.5 flex items-center gap-2.5 rounded-[10px] px-3 py-2 text-mist hover:bg-panel hover:text-foreground"><span className="size-1.5 rounded-full bg-hairline" />{item}</a>)}</nav><div className="mt-auto border-t border-hairline p-3"><div className="flex items-center gap-3 rounded-[12px] bg-panel px-3 py-2.5 ring-1 ring-inset ring-white/5"><img src={profile?.avatar_url || instructorRafael} alt="Perfil" width={512} height={512} className="size-9 rounded-full object-cover"/><div className="min-w-0 flex-1 leading-tight"><p className="truncate text-[13px] font-medium">{profile?.full_name || "Usuário"}</p><p className="font-mono text-[10px] text-faint">{roleLabel}</p></div><button onClick={logout} title="Sair" className="text-faint hover:text-foreground"><LogOut className="size-4" /></button></div></div></aside>
  <main className="min-w-0 flex-1"><header className="flex h-16 items-center gap-4 border-b border-hairline bg-obsidian/85 px-4 backdrop-blur-sm sm:px-6"><div><h1 className="text-[15px] font-semibold">{isStudent ? "Meu espaço" : "Visão geral"}</h1><p className="mt-1 font-mono text-[10px] uppercase tracking-[.15em] text-faint">Gestão FMP BJJ</p></div><div className="ml-auto flex items-center gap-2">{!isStudent && <><Button variant="panel" size="sm" onClick={() => setNotice("Nova aula pronta para ser criada.")}><CalendarPlus /><span className="hidden sm:inline">Nova aula</span></Button><Link to="/alunos" className="inline-flex h-9 items-center gap-2 rounded-lg bg-primary px-3 text-xs font-semibold text-primary-foreground hover:brightness-110"><Plus className="size-4"/><span className="hidden sm:inline">Cadastrar aluno</span></Link></>}</div></header>
  <div className="mx-auto max-w-[1440px] px-4 py-5 sm:px-6 sm:py-6">{notice && <div className="mb-3 flex items-center justify-between rounded-[10px] border border-primary/30 bg-primary/10 px-3 py-2 text-xs text-accent-soft"><span>{notice}</span><button onClick={() => setNotice("")}><Check className="size-4"/></button></div>}<section className="grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-4">{(isStudent ? [["Próxima aula","Hoje","18:30","Jiu-Jitsu · Fundamentos"],["Presença","Este mês","92%","Excelente frequência"],["Graduação","Atual","Azul","1º grau"],["Mensalidade","Status","OK","Próximo vencimento"]] : [["Alunos ativos","+8","247","63% matriculados no ciclo atual"],["Aulas de hoje","6 / 8","6","Pico às 19h · turma de Jiu-Jitsu"],["Receita mensal","+12,4%","R$ 68.420","Projeção do trimestre: R$ 214k"],["Presença média","7 dias","82%","Jiu-Jitsu lidera com 91%"]]).map(([label,badge,value,text],i)=><article key={label} className="spec-surface rounded-[16px] bg-panel/70 p-5 ring-1 ring-inset ring-white/5"><div className="flex justify-between"><span className="font-mono text-[10px] uppercase tracking-[.18em] text-faint">{label}</span><span className="rounded-full bg-primary/15 px-2 py-0.5 font-mono text-[10px] text-accent-soft">{badge}</span></div><p className="mt-3 font-mono text-3xl font-semibold">{value}</p><p className="mt-2 text-[12px] text-faint">{text}</p></article>)}</section><section className="mt-3 grid grid-cols-1 gap-3 lg:grid-cols-3"><article className="spec-surface rounded-[16px] bg-panel/70 p-5 ring-1 ring-inset ring-white/5 lg:col-span-2"><div className="flex items-center justify-between"><div><h2 className="text-[15px] font-semibold">{isStudent ? "Minha agenda" : "Agenda de treinos"}</h2><p className="mt-1.5 font-mono text-[10px] uppercase tracking-[.15em] text-faint">Hoje · 6 sessões programadas</p></div><Button variant="panel" size="sm">Semana <ChevronDown/></Button></div><div className="mt-4 divide-y divide-hairline">{classes.map(([time,title,teacher,count,active])=><div key={time} className="flex items-center gap-4 py-3"><span className={active?"w-12 font-mono text-[12px] text-accent-soft":"w-12 font-mono text-[12px] text-mist"}>{time}</span><div className="min-w-0 flex-1"><p className="truncate text-[13px] font-medium">{title}</p><p className="mt-0.5 truncate font-mono text-[10px] text-faint">{teacher}</p></div><span className="rounded-full bg-white/5 px-2.5 py-1 font-mono text-[10px] text-mist">{count}</span></div>)}</div></article><article className="spec-surface rounded-[16px] bg-panel/70 p-5 ring-1 ring-inset ring-white/5"><h2 className="text-[15px] font-semibold">{isStudent?"Meu acesso":"Presenças recentes"}</h2><p className="mt-1.5 font-mono text-[10px] uppercase tracking-[.15em] text-faint">{isStudent?roleLabel:"Últimos check-ins"}</p>{!isStudent&&<div className="mt-4 space-y-2.5">{attendees.map(([photo,name,discipline,time])=><div key={name} className="flex items-center gap-3 rounded-[12px] bg-obsidian/70 px-3 py-2.5 ring-1 ring-inset ring-white/5"><img src={photo} alt={name} width={512} height={512} className="size-9 rounded-full object-cover"/><div className="min-w-0 flex-1"><p className="truncate text-[13px] font-medium">{name}</p><p className="font-mono text-[10px] text-faint">{discipline}</p></div><span className="font-mono text-[10px] text-mist">{time}</span></div>)}</div>}<Button variant="panel" className="mt-4 w-full" onClick={()=>setNotice(isStudent?"Seu histórico será exibido aqui.":"Exibindo todas as presenças de hoje.")}>{isStudent?"Ver minha frequência":<><Users/>Ver todas as presenças</>}</Button></article></section></div></main></div></div>;
}
